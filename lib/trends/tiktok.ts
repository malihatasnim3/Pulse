/**
 * Placeholder for TikTok Creative Center / Marketing API ingestion.
 * Wire the official API for production trend pulls.
 */
export async function fetchTiktokCreativeCenter(): Promise<unknown[]> {
  return [];
}
